% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Plot.R
\name{id_plot_items}
\alias{id_plot_items}
\title{Plot Item Discrimination or Difficulty Parameters}
\usage{
id_plot_items(
  object,
  param_type = "discrimination",
  item_type = "both",
  include = NULL,
  high_limit = 0.95,
  low_limit = 0.05,
  text_size_label = 2,
  point_size = 1,
  item_labels = TRUE,
  order_by = "median",
  return_data = FALSE,
  show_true = FALSE,
  use_chain = NULL,
  ...
)
}
\arguments{
\item{object}{A fitted \code{idealstan} object}

\item{param_type}{Which parameter to plot: \code{"discrimination"} (default) or \code{"difficulty"}}

\item{item_type}{Which item parameters to show: \code{"both"} (default), \code{"observed"}, or \code{"missing"}.
\code{"observed"} shows the non-inflated parameters (gamma/beta), \code{"missing"} shows the
inflated/absence parameters (nu/omega).}

\item{include}{A character vector of item IDs to include in the plot (all others excluded).
If NULL (default), all items are shown.}

\item{high_limit}{The quantile (number between 0 and 1) for the high end of posterior
uncertainty to show in plot. Default is 0.95.}

\item{low_limit}{The quantile (number between 0 and 1) for the low end of posterior
uncertainty to show in plot. Default is 0.05.}

\item{text_size_label}{The size of text labels for item names. Default is 2.}

\item{point_size}{The size of points in the plot. Default is 1.}

\item{item_labels}{If \code{TRUE} (default), show item name labels on the plot.}

\item{order_by}{How to order items: \code{"median"} (default) orders by posterior median,
\code{"name"} orders alphabetically by item name, \code{"uncertainty"} orders by width of
posterior interval.}

\item{return_data}{If \code{TRUE}, return the data used for plotting along with the plot
as a list. Default is \code{FALSE}.}

\item{show_true}{If the model was fitted on simulated data, show the true parameter values.
Default is \code{FALSE}.}

\item{use_chain}{Which MCMC chains to use. Default is NULL (use all chains).}

\item{...}{Other arguments (currently ignored)}
}
\value{
A \code{ggplot2} object, or if \code{return_data=TRUE}, a list with elements \code{plot} and \code{data}.
}
\description{
This function plots item-level parameters (discrimination or difficulty) from a
fitted \code{idealstan} object. It can display both observed (non-inflated) and
missing (inflated) item parameters.
}
\examples{
\dontrun{
# After fitting a model:
# Plot discrimination parameters for all items
id_plot_items(fitted_model)

# Plot only difficulty parameters
id_plot_items(fitted_model, param_type = "difficulty")

# Plot only observed (non-inflated) discrimination
id_plot_items(fitted_model, item_type = "observed")

# Plot specific items only
id_plot_items(fitted_model, include = c("item1", "item2", "item3"))
}

}
